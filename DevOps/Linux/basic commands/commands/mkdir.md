**mkdir** command is used to create one or more new directories in the file system.

- rm: Short for remove. It is the standard command to delete files.

- -r (recursive): Tells the system to go into directories and delete everything inside, including subfolders and their files.

- -f (force): Tells the system to ignore permissions and never ask for confirmation. It bypasses any "Are you sure?" prompts.