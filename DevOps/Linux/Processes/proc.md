**/proc** — это виртуальная псевдофайловая система в Linux/Unix, выступающая интерфейсом к ядру и процессам. Она не занимает место на диске, а генерируется в оперативной памяти (RAM) на лету.

**ps aux** и **htop** берут данные именно из /proc.